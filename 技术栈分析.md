# Personal-toolbox 技术栈分析文档

> 本文档基于项目源码（package.json、vite.config.js、src/、.github/workflows/ 等）整理，描述当前版本（v0.1.0）的技术选型、UI 风格与数据存储方案。

***

## 一、项目概述

Personal-toolbox（个人工具箱）是一个**纯前端**工具集网站：

* 所有工具在**浏览器本地运行**，不依赖后端服务器；

* 通过 **GitHub Actions 自动构建并托管在 GitHub Pages**，构建产物使用相对路径，可部署到任意仓库子路径下；

* 可选接入**用户自己的 Gitee 仓库**作为云端存档（"类数据库"），数据只保存在用户自己的仓库中，不上传至任何第三方服务器；

  <br />

扩展方式：在 `src/tools/` 下创建组件目录，并在 `src/tools/index.jsx` 的工具注册表中注册一条记录即可。

***

## 二、核心技术栈

### 2.1 框架与运行时

| 类别        | 技术                   | 版本      | 说明                                                  |
| --------- | -------------------- | ------- | --------------------------------------------------- |
| 构建工具      | Vite                 | ^6.2.0  | 开发服务器 + 生产构建，ESM 原生支持                               |
| UI 框架     | React                | ^18.3.1 | 函数组件 + Hooks，`React.StrictMode` 严格模式                |
| React DOM | react-dom            | ^18.3.1 | `createRoot` 并发渲染入口                                 |
| 路由        | react-router-dom     | ^6.30.0 | 使用 **HashRouter**（URL 形如 `#/tools/...`），刷新子路由不会 404 |
| React 插件  | @vitejs/plugin-react | ^4.3.4  | Fast Refresh（开发）/ JSX 编译（构建）                        |

### 2.2 UI 组件库与样式

| 类别   | 技术                    | 版本       | 说明                                                     |
| ---- | --------------------- | -------- | ------------------------------------------------------ |
| 组件库  | Ant Design            | ^5.24.6  | 全部界面基于 antd 组件体系                                       |
| 图标   | @ant-design/icons     | ^5.6.1   | 工具图标、操作图标                                              |
| 日期库  | dayjs                 | ^1.11.23 | antd 日期组件的底层依赖，同时用于日历推算                                |
| 样式重置 | antd/dist/reset.css   | -        | antd 5 官方重置样式                                          |
| 主题语言 | ConfigProvider + zhCN | -        | 全局中文语言包（`antd/locale/zh_CN`），dayjs 显式切换 `zh-cn` locale |

> 项目**未使用** CSS 框架（Tailwind / Less / Sass）或独立样式文件，样式以内联 `style` 对象 + antd 设计令牌（`theme.useToken()`）为主，风格轻量、与 antd 默认视觉一致。

### 2.3 功能类库

| 库     | 版本           | 用途                                 |
| ----- | ------------ | ---------------------------------- |
| jszip | ^3.10.1      | 文件夹批量处理 PNG 时打包 ZIP 供本地下载          |
| sharp | ^0.35.4（dev） | 构建辅助：从 favicon.svg 光栅化生成 PWA 各尺寸图标 |

***

## 三、UI 风格与交互设计

### 3.1 整体视觉

* **主题**：Ant Design 5 默认浅色主题，品牌蓝 `#1677ff`（与 PWA `theme_color`、favicon 一致）；

* **语言**：全站中文（`index.html` 声明 `lang="zh-CN"`，antd 与 dayjs 均为中文 locale）；

* **布局**：经典后台三段式 —— `Layout` 嵌套 `Sider`（侧边导航）+ `Header`（页头）+ `Content`（内容卡片），内容区为白底、圆角（`borderRadiusLG`）卡片；

* **分隔线**：统一使用 `1px solid #f0f0f0` 的浅灰边框；

* **图标风格**：侧边栏 Logo 与首页工具图标采用"浅蓝底 + 品牌蓝图标"的圆角方块（`#e6f4ff` 底、`#1677ff` 图标、40×40、圆角 10px）。

### 3.2 导航交互（特色设计）

* **导航默认隐藏**：左侧导航初始为折叠态（`collapsedWidth={0}`），点击页头折叠按钮展开；

* **自动收起**：路由切换后自动收起导航；点选菜单进入页面后立即收起（`useEffect` 监听 `location.pathname`）；

* **防默认弹出**：折叠态下强制 `openKeys=[]`，避免 antd Menu 在 vertical 模式下判定子菜单展开、首屏左上角弹出悬浮卡片；

* **页头**：左侧为折叠按钮 + 当前页面标题（随路由自动切换），右侧为 Gitee 同步状态 Tag（绿=已配置 / 默认=未配置，Tooltip 提示说明）+ "Gitee 配置"按钮。

### 3.3 响应式适配规范（手机 / 平板）

全站以 antd Grid 断点为准，**md（768px）是「手机 ↔ 桌面（含平板）」的唯一分界**，不另设自定义断点：

| 视口宽度    | 设备定位   | 判定（`Grid.useBreakpoint()`） | 布局策略                   |
| ----------- | ---------- | ------------------------------ | -------------------------- |
| 小于 768px  | 手机       | `!screens.md`                  | 紧凑布局 + 浮层导航 + 单列 |
| 768–991px   | 平板（md） | `screens.md && !screens.lg`    | 桌面布局，多列收敛为 2 列  |
| 992px 及以上 | 桌面（lg+） | `screens.lg`                  | 推挤式导航 + 多列栅格      |

平板端（md）采用桌面布局但视口偏窄（扣除推挤导航后内容区约 500–730px）：多列排布按 md 断点收敛（如 `xs={24} md={12}` 在平板呈 2 列），避免在 md 区间出现 3 列以上卡片。

#### 3.3.1 全局壳层行为（App.jsx 已实现，改版时保持）

* 手机端：导航为**浮层覆盖式**（`position: fixed` + 阴影 + 半透明遮罩，点击空白处收起），不挤压内容；Header、Content 的内边距压缩（12px）；页头状态标签与文字按钮降级为仅图标（Tooltip / title 保留提示）；

* 桌面与平板端：导航为推挤式布局，内容留白 24px；

* 首页卡片栅格自适应：`xs=24`（手机单列）/ `sm=12`（平板两列）/ `lg=8` / `xl=6`。

#### 3.3.2 工具页面适配清单（新增工具必须遵循，变更 UI 时对照回归）

1. **栅格布局**：多列/多卡排布一律用 `Row` + `Col` 响应式断点，禁止固定像素宽度的横向多列；现有约定——卡片列表 `xs={24} sm={12} xl={8}`、表单并排 `xs={24} md={12}`、详情主副栏 `xs={24} md={10/14}`；
2. **工具栏与按钮组**：antd `Space` 必须加 `wrap`；自定义 flex 容器加 `flexWrap: 'wrap'`——默认不换行是窄屏横向溢出的最常见原因；
3. **固定宽度元素**：手机端改为占满整行或自适应（局部用 `Grid.useBreakpoint()` 判断，如知识库搜索框 `width: isMobile ? '100%' : 300`）；
4. **表格**：列较多时设置 `scroll={{ x: 'max-content' }}`（借贷账簿还款记录表即此模式），避免列宽撑破容器；
5. **描述列表**：`Descriptions` 使用响应式 `column`（如 `{ xs: 1, sm: 2 }`、`{ xs: 2, md: 4 }`）；
6. **弹窗 / 抽屉**：依赖 antd 内置 `max-width: calc(100vw - 32px)` 自动收缩，无需自设小屏宽度；不要在弹窗内嵌固定大宽度的内联元素；
7. **长文本**：省略展示（`ellipsis` / line-clamp）+ Tooltip 查看全文，或 `whiteSpace: 'pre-wrap'` 自然换行，避免强制单行撑宽。

#### 3.3.3 验收标准（功能开发完成前自检）

* 在约 **375px（手机）** 与 **768px（平板）** 两个视口下：页面无横向滚动条（无横向溢出）；

* 关键操作（新建、编辑、删除、主按钮）在窄屏完整可达，不被遮挡或裁切；

* 开发版与构建产物行为一致：可用 `npm run preview`（4173）或局域网真机访问 dev（见 6.1）复核。

### 3.4 首页工具卡片

* 卡片 `hoverable`，点击跳转对应工具路由；

* 卡头：圆角图标块 + 工具名；卡身：两行省略的描述文本（CSS line-clamp），悬浮时 Tooltip 展示完整描述（通过测量 `scrollHeight` 判断是否溢出，字体加载后 300ms 复测）；

* 顶部说明文字强调隐私特性："工具均在本地浏览器中运行……数据不会上传到其他任何服务器"。

***

## 四、数据存储方案

### 4.1 分层存储策略总览

项目没有自建后端，采用「**localStorage 离线缓存 + Gitee 仓库云存档**」的双层方案（各工具一致）：

```
┌──────────────────────────┐
│  工具（浏览器，纯前端）    │
│                          │
│  读写即时 → localStorage │  ← 第 1 层：本地缓存（离线可用、即写即存）
│         ⇅ 进入拉取/变更推送│
│  Gitee API v5（HTTPS）    │  ← 第 2 层：云存档（主数据，用户自己的仓库）
└──────────────────────────┘
```

**设计原则**：

1. **云端为主、本地为辅**：Gitee 仓库为权威存档（跨设备可见），localStorage 为离线缓存与会话降级方案；
2. **隐私优先**：Gitee 仅存用户自己仓库；未配置 Gitee 时全部功能仅本地可用（页头显示"未配置历史存储"，知识库显示"仅本地"标签）；
3. **容错自愈**：首次使用、仓库无存档、目录不存在均不报错，写入时自动创建；存档损坏自动备份原始内容并重置；
4. **降级可用**：云端请求失败时工具降级使用本地缓存并明确提示，提供手动重试，不阻塞核心功能。

### 4.2 第一层：localStorage 本地缓存

| Key                                     | 用途                                        |
| --------------------------------------- | ----------------------------------------- |
| `toolbox.giteeConfig`                   | Gitee 连接配置（owner / repo / token / branch） |
| `cycleTracker:v1`                       | 周期记录工具完整数据（含 `version: 1` 结构版本号）          |
| `cycleTracker:v1:corrupt-backup`        | 周期记录本地数据损坏时的原始内容备份                        |
| `knowledgeBase:v1`                      | 知识库整库缓存（与云端结构一致）                          |
| `knowledgeBase:v1:corrupt-backup`       | 知识库本地数据损坏备份                               |
| `knowledgeBase:v1:cloud-corrupt-backup` | 知识库云端某分类文件损坏时的原始内容备份                      |
| `loanLedger:v1`                         | 借贷账簿完整数据（含 `version: 1` 结构版本号）                |
| `loanLedger:v1:corrupt-backup`          | 借贷账簿本地数据损坏时的原始内容备份                        |

* 数据均带 `version` 字段，便于后续结构升级；

* 写入失败仅 `console.warn` 告警、不抛错，避免阻塞使用；

* 读取损坏时自动备份原内容 → 重置为空数据 → `recovered: true` 供 UI 提示。

### 4.3 第二层：Gitee 仓库云存档

#### 4.3.1 API 封装（src/services/gitee.js）

基于 **Gitee API v5 REST 接口**（`https://gitee.com/api/v5`），使用私人令牌 `access_token` 鉴权，所有请求 `cache: 'no-store'` 保证读到最新状态：

| 函数                                    | 能力                                                              |
| ------------------------------------- | --------------------------------------------------------------- |
| `getRepoInfo`                         | 查询仓库信息（"测试连接"、获取默认分支）                                           |
| `readRepoFile` / `readRepoFileBinary` | 读取文本 / 二进制文件（如图片）                                               |
| `writeRepoFile`                       | 写入文件：已存在 PUT + sha 更新，不存在 POST 创建（父目录自动创建）；**409 sha 冲突自动重试一次** |
| `deleteRepoFile`                      | 删除文件（不存在视为已删除，不抛错）                                              |

**健壮性处理**（均来自实测踩坑）：

* Gitee 对"文件/分支不存在"返回 **HTTP 200 + 空数组**而非 404，封装层兼容两种形态，统一按"首次使用"处理；

* 路径对应目录（非空数组）时抛出可操作错误（提示删除同名文件夹）；

* Base64 编解码兼容 RFC 2045 换行格式，二进制写入分块拼接避免大文件性能问题。

#### 4.3.2 存档目录结构

所有工具存档统一放在仓库 **`history/` 目录**下，**按工具 ID 分目录**（`toolId` 由工具路由末段推导，如 `/tools/png-alpha-normalize` → `png-alpha-normalize`）：

```
history/
├── png-alpha-normalize/
│   ├── history.json          # 使用历史（最近 20 次，记录含参数与结果图文件名）
│   └── results/              # 单张处理的结果图（保留最近 20 张，被挤出的自动清理）
├── cycle-tracker/
│   ├── history.json          # 使用历史（最近 20 条）
│   └── data.json             # 完整日历存档（calendars → events / records）
├── loan-ledger/
│   ├── history.json          # 使用历史（最近 20 条）
│   └── data.json             # 完整借贷存档（items → 借贷事项 + repayments 还款记录）
└── knowledge-base/
    ├── history.json          # 使用历史（最近 20 条）
    ├── index.json            # 索引：{ version, defaultCategoryId, categories: [{id, name}] }
    └── <分类名>.json          # 按分类拆分的卡片文件；默认分类「未分类」固定为 default.json
```

### 4.4 各工具存储明细

| 工具         | 云存档路径                                                   | 本地缓存                           | 同步策略                                                                                                                 |
| ---------- | ------------------------------------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| PNG 透明度归一化 | `history/png-alpha-normalize/history.json` + `results/` | 仅配置项在 localStorage（处理结果本身不落本地） | 单张处理自动同步记录与结果图；单张结果图 > 900KB 仅存记录不上传；历史写入经**模块级串行队列**防互相覆盖                                                           |
| 周期记录与预测    | `history/cycle-tracker/data.json` + `history.json`      | `cycleTracker:v1`              | 进入工具读云端（主存档）；数据变更即时写本地、并推送云端；仓库无存档时自动创建空档（本地有旧数据则先弹窗询问上传或忽略）；云端不可用时本会话降级本地并提示                                        |
| 个人知识库      | `history/knowledge-base/index.json` + `<分类名>.json`      | `knowledgeBase:v1`             | 进入拉取 → 以内容哈希（`contentKey`，忽略易变的 `lastViewedAt`）做**文件级 diff** 决定推送哪些文件；写入经入口组件的**串行写队列**（`enqueuePush`）；多端冲突**以较新为准** |
| 借贷账簿       | `history/loan-ledger/data.json` + `history.json`        | `loanLedger:v1`                | 进入工具读云端（主存档）；数据变更即时写本地并推送云端；仓库无存档时自动创建空档（本地有旧数据则先弹窗询问上传或忽略）；云端不可用时本会话降级本地并提示 |

### 4.5 通用可靠性机制

* **使用历史统一治理**：`src/services/history.js` 提供 `getToolHistory` / `recordToolUsage`，每次使用插入头部、自动裁剪至最近 **20 条**（`HISTORY_RECORD_LIMIT`），首次使用自动创建存档；

* **串行写队列**：同一工具的所有云端写入排队执行，避免并发 PUT 互相覆盖（周期记录、PNG、知识库各自实现）;

* **冲突重试**：写文件遇 409（sha 过期）自动重读重写一次；

* **损坏自愈**：本地 / 云端 JSON 解析失败 → 备份原始内容 → 按空数据处理，不阻塞功能；

* **手动重试**：降级本地后 UI 提供"重试同步"入口，用户可恢复云端同步。

***

## 五、PWA 支持

基于 **vite-plugin-pwa（^1.3.0）** 实现，构建期自动生成 `manifest.webmanifest` 与 Workbox Service Worker：

* **更新策略**：`registerType: 'autoUpdate'`，部署新版本后用户无感知升级；

* **安装体验**：`display: standalone`，名称"个人工具箱 / 工具箱"，`theme_color: #1677ff`；`start_url` / `scope` 使用相对路径（`./`）兼容 GitHub Pages 子路径；

* **图标体系**：`pwa-192x192.png`、`pwa-512x512.png`、`maskable-icon-512x512.png`（Android 自适应）、`apple-touch-icon-180x180.png`（iOS）、`favicon.svg`（由 sharp 从 SVG 光栅化生成）；

* **离线能力**：Workbox 预缓存全部 `js/css/html` 构建产物（`cleanupOutdatedCaches` 自动清理过期缓存），已安装应用可离线打开；

* **开发体验**：`devOptions.enabled: true`，localhost 开发时即可体验"安装到桌面"（因相对 base 需显式指定 `webManifestUrl: '/manifest.webmanifest'`）；

* **安装引导**：`PwaInstallPrompt` 组件监听 `beforeinstallprompt`，检测到可安装时自动弹窗引导。

***

## 六、构建与部署

### 6.1 Vite 构建配置（vite.config.js）

| 配置                                        | 值                         | 目的                                 |
| ----------------------------------------- | ------------------------- | ---------------------------------- |
| `base`                                    | `'./'`                    | 相对路径构建，产物可部署到 GitHub Pages 任意仓库子路径 |
| `build.rollupOptions.output.manualChunks` | `react` / `antd` 两个 chunk | 框架依赖拆分，利于浏览器缓存与首屏加载                |
| `server.host`                             | `true`                    | 监听 0.0.0.0，同一 Wi-Fi 下手机可访问开发服务     |
| `server.port`                             | `5173`                    | 开发端口（preview 默认 4173）              |

### 6.2 CI/CD（.github/workflows/deploy.yml）

* **触发**：推送到 `main` 分支，或 Actions 页面手动触发（`workflow_dispatch`）；

* **流程**：`actions/checkout@v4` → Node 20（npm 缓存）→ `npm ci` → `npm run build` → `upload-pages-artifact@v3`（上传 `dist/`）→ `deploy-pages@v4` 发布到 GitHub Pages；

* **并发控制**：`concurrency: pages` + `cancel-in-progress`，新推送取消进行中的旧部署；

* 首次部署需在仓库 Settings 中开启 Pages（Source: GitHub Actions）并授予 Actions 读写权限。

### 6.3 常用脚本

```bash
npm install
npm run dev        # 本地开发（http://localhost:5173，支持局域网访问）
npm run build      # 生产构建（输出到 dist/）
npm run preview    # 预览构建产物（http://localhost:4173）
```

***

## 七、工具模块架构与扩展方式

### 7.1 目录结构（src/）

```
src/
├── main.jsx                  # 入口：ConfigProvider(zhCN) + HashRouter + dayjs 中文 locale
├── App.jsx                   # 全局布局：Sider 导航 / Header / Content / 路由分发
├── pages/Home.jsx            # 首页：工具卡片网格
├── components/
│   ├── GiteeSettings.jsx     # Gitee 连接配置弹窗（测试连接 / 保存 / 清除）
│   └── PwaInstallPrompt.jsx  # PWA 安装引导弹窗
├── services/
│   ├── gitee.js              # Gitee API v5 封装（仓库查询 / 文件读写删 / base64 编解码）
│   └── history.js            # 通用使用历史服务（按工具分目录，保留 20 条）
└── tools/
    ├── index.jsx             # 工具注册表（path / name / desc / icon / component / tag）
    ├── pngAlphaNormalize/    # PNG 透明度归一化
    ├── cycleTracker/         # 周期记录与预测（cycleAlgo.js 纯算法 + storage.js 存储 + 视图组件）
    ├── loanLedger/           # 借贷账簿（loanCore.js 纯逻辑 + loanStorage.js 存储 + 5 个视图组件）
    └── knowledgeBase/        # 个人知识库（kbCore.js 纯逻辑 + kbStorage.js 存储 + 4 个视图组件）
```

### 7.2 分层模式

每个工具模块遵循「**纯逻辑层 / 存储层 / 视图层**」分离：

* **纯逻辑层**（如 `cycleAlgo.js`、`kbCore.js`）：日期与周期推算、卡片 id、校验、搜索排序等纯函数，无副作用、易测试；

* **存储层**（如 `storage.js`、`kbStorage.js`、`kbCore.js`+`kbStorage.js` 组合）：封装 localStorage 缓存与 Gitee 存档的读写、序列化、损坏修复；

* **视图层**（`*.jsx`）：antd 组件实现的交互界面，调度存储层与逻辑层。

### 7.3 新增工具步骤

1. 在 `src/tools/` 下创建工具目录（建议按上节三层拆分）；
2. 实现工具组件，如需云存档则复用 `services/gitee.js`，按 `history/<toolId>/` 目录规划存档文件；
3. 按 3.3 的适配清单完成手机 / 平板适配自检（栅格、Space wrap、表格 scroll、固定宽度元素、描述列表、弹窗等）；
4. 在 `src/tools/index.jsx` 注册：`path`（唯一路由）、`name`、`desc`（首页卡片描述）、`icon`、`component`、可选 `tag`（如 `'NEW'`）；
5. 首页卡片、侧边导航、路由会自动生效（均基于注册表驱动）。

***

## 八、开发实践约定（摘要）

* **非阻塞异步**：涉及云端同步的核心操作不阻塞 UI，失败降级 + 提示 + 手动重试；

* **竞态处理**：云端写入使用串行队列，读取带 `no-store`，多端冲突以较新为准；初始化注入数据与自动持久化副作用同轮触发时，持久化需以“与存档基线等值对齐后”才进入变更跟踪（catch-up 模式），避免用初始旧状态覆盖本地缓存；

* **本地日期字符串**：周期工具统一用 `'YYYY-MM-DD'` 本地日期作 key，规避 UTC/时区偏移；

* **响应式回归**：新增功能或变更 UI 后，按 3.3.3 在约 375px（手机）与 768px（平板）视口自检——无横向溢出、关键操作可达；

* **GitHub Pages 适配**：`base: './'` + HashRouter，保证子路径部署与子路由刷新可用；

* **UI 自动化验证**：`scripts/` 目录保留了大量 Playwright 风格的验证截图，用于核心交互回归检查。

***

## 九、技术选型总结

| 维度  | 选型                                               | 特点                     |
| --- | ------------------------------------------------ | ---------------------- |
| 架构  | 纯前端 SPA（无自建后端）                                   | 隐私安全、零服务器成本、部署简单       |
| 框架  | Vite 6 + React 18                                | 启动快、HMR 体验好、生态成熟       |
| UI  | Ant Design 5（中文 zhCN，浅色主题，品牌蓝 #1677ff）           | 开箱即用的企业级组件、移动端响应式良好    |
| 路由  | HashRouter + 相对路径 base                           | 完美适配 GitHub Pages 静态托管 |
| 数据  | localStorage + Gitee 仓库（按工具分目录、JSON 存档、20 条历史上限） | 免后端持久化，跨设备同步，数据完全归用户所有 |
| PWA | vite-plugin-pwa + Workbox                        | 可安装到桌面、离线可用、自动更新       |
| 部署  | GitHub Actions → GitHub Pages                    | 推送即部署，零运维              |
| 扩展  | 工具注册表 + 三层模块化（逻辑/存储/视图）                          | 新增工具成本低、易于             |
